name: Rust Release

on:
  workflow_call:
    inputs:
      binary-name:
        description: "Binary/crate name (e.g. texforge, gitkit)"
        type: string
        required: true
      system-packages:
        description: "Space-separated apt packages to install before building (e.g. 'protobuf-compiler libssl-dev')"
        type: string
        default: ""
      include-windows:
        description: "Build and release a native Windows (x86_64-pc-windows-msvc) binary"
        type: boolean
        default: true

permissions:
  contents: write

# Serialize releases per caller repo — never cancel a release mid-flight, but
# don't let two run at once and race on tag/publish.
concurrency:
  group: rust-release-${{ github.repository }}
  cancel-in-progress: false

env:
  CARGO_TERM_COLOR: always

jobs:
  create-tag:
    name: Create Release Tag
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.version.outputs.tag }}
      version: ${{ steps.version.outputs.version }}
      created: ${{ steps.create.outputs.created }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Get version from Cargo.toml
        id: version
        run: |
          VERSION=$(grep '^version = ' Cargo.toml | head -1 | sed 's/version = "\(.*\)"/\1/' | tr -d '\r' | xargs)
          TAG="v${VERSION}"
          echo "version=${VERSION}" >> $GITHUB_OUTPUT
          echo "tag=${TAG}" >> $GITHUB_OUTPUT

      - name: Check if tag already exists
        id: check_tag
        run: |
          if git rev-parse "${{ steps.version.outputs.tag }}" >/dev/null 2>&1; then
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Create and push tag
        id: create
        run: |
          if [ "${{ steps.check_tag.outputs.exists }}" = "true" ]; then
            echo "created=false" >> $GITHUB_OUTPUT
            echo "Tag ${{ steps.version.outputs.tag }} already exists — skipping"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag -a "${{ steps.version.outputs.tag }}" -m "Release ${{ steps.version.outputs.tag }}"
          git push origin "${{ steps.version.outputs.tag }}"
          echo "created=true" >> $GITHUB_OUTPUT

  build:
    name: Build ${{ matrix.target }}
    needs: create-tag
    if: needs.create-tag.outputs.created == 'true'
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-musl
            os: ubuntu-latest
            archive: tar.gz
          - target: aarch64-apple-darwin
            os: macos-latest
            archive: tar.gz
          - target: x86_64-apple-darwin
            os: macos-latest
            archive: tar.gz
          - target: x86_64-pc-windows-msvc
            os: windows-latest
            archive: zip
    steps:
      - if: matrix.os == 'windows-latest' && !inputs.include-windows
        name: Skip Windows build
        run: |
          echo "Windows build disabled via include-windows: false — skipping"
          exit 0
        shell: bash

      - uses: actions/checkout@v4
        if: matrix.os != 'windows-latest' || inputs.include-windows
        with:
          ref: ${{ needs.create-tag.outputs.tag }}

      - uses: dtolnay/rust-toolchain@stable
        if: matrix.os != 'windows-latest' || inputs.include-windows
        with:
          targets: ${{ matrix.target }}

      - name: Install system packages
        if: (matrix.os != 'windows-latest' || inputs.include-windows) && runner.os == 'Linux' && inputs.system-packages != ''
        run: |
          sudo apt-get update
          sudo apt-get install -y ${{ inputs.system-packages }}

      - name: Install musl tools
        if: (matrix.os != 'windows-latest' || inputs.include-windows) && matrix.target == 'x86_64-unknown-linux-musl'
        run: sudo apt-get update && sudo apt-get install -y musl-tools

      - uses: Swatinem/rust-cache@v2
        if: matrix.os != 'windows-latest' || inputs.include-windows
        with:
          cache-on-failure: true

      - name: Build
        if: matrix.os != 'windows-latest' || inputs.include-windows
        run: cargo build --locked --release --target ${{ matrix.target }}

      - name: Package (unix)
        if: (matrix.os != 'windows-latest' || inputs.include-windows) && matrix.archive == 'tar.gz'
        run: |
          cd target/${{ matrix.target }}/release
          tar czf "../../../${{ inputs.binary-name }}-${{ needs.create-tag.outputs.tag }}-${{ matrix.target }}.tar.gz" ${{ inputs.binary-name }}

      - name: Package (windows)
        if: inputs.include-windows && matrix.archive == 'zip'
        shell: pwsh
        run: |
          cd target/${{ matrix.target }}/release
          Compress-Archive -Path ${{ inputs.binary-name }}.exe -DestinationPath "../../../${{ inputs.binary-name }}-${{ needs.create-tag.outputs.tag }}-${{ matrix.target }}.zip"

      - name: Upload artifact
        if: matrix.os != 'windows-latest' || inputs.include-windows
        uses: actions/upload-artifact@v4
        with:
          name: ${{ inputs.binary-name }}-${{ matrix.target }}
          path: ${{ inputs.binary-name }}-${{ needs.create-tag.outputs.tag }}-${{ matrix.target }}.*

  github-release:
    name: Create GitHub Release
    needs: [create-tag, build]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      # One checksum file users (and the install scripts) can verify against.
      - name: Generate checksums
        run: |
          cd artifacts
          sha256sum * > SHA256SUMS.txt
          cat SHA256SUMS.txt

      - uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.create-tag.outputs.tag }}
          generate_release_notes: true
          files: artifacts/*

  approve:
    name: Awaiting Manual Approval
    needs: github-release
    runs-on: ubuntu-latest
    environment:
      name: production
    steps:
      - run: echo "Release approved for publishing to crates.io"

  ensure-owners:
    name: Ensure crates.io owners
    needs: create-tag
    if: needs.create-tag.outputs.created == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.create-tag.outputs.tag }}

      - uses: dtolnay/rust-toolchain@stable

      - name: Add crates.io owners (best-effort)
        env:
          TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: |
          if [ -z "$TOKEN" ]; then
            echo "No CARGO_REGISTRY_TOKEN — skipping owners update"
            exit 0
          fi
          cargo owner --token "$TOKEN" --add github:univerlab:owners ${{ inputs.binary-name }} || true

  publish:
    name: Publish to crates.io
    needs: [create-tag, approve, ensure-owners]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.create-tag.outputs.tag }}

      - uses: dtolnay/rust-toolchain@stable

      - name: Install system packages
        if: inputs.system-packages != ''
        run: |
          sudo apt-get update
          sudo apt-get install -y ${{ inputs.system-packages }}

      - name: Check stable version
        id: version_check
        run: |
          VERSION=${{ needs.create-tag.outputs.version }}
          if [[ "$VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "is_stable=true" >> $GITHUB_OUTPUT
          else
            echo "is_stable=false" >> $GITHUB_OUTPUT
            echo "Version $VERSION is prerelease — skipping publish"
          fi

      - name: Cargo publish
        if: steps.version_check.outputs.is_stable == 'true'
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: cargo publish --locked --allow-dirty
